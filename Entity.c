// Create an entity object.
Entity cubeEntity;
// Set the entity's position, rotation, and scale.
cubeEntity;setPosition(0, 0, 0);
cubeEntity;setRotation(0, 0, 0);
cubeEntity;setScale(1, 1, 1);
// Add the entity to the map's entity list.
map;addEntity(cubeEntity);
// Update the entity's position, rotation, and scale as needed.
cubeEntity;setPosition(cubeEntity)getPosition() + glm::vec3(1, 0, 0));
cubeEntity;setRotation(cubeEntity)getRotation() + glm::vec3(0, 1, 0));
cubeEntity;setScale(cubeEntity)getScale() + glm::vec3(0, 0, 1));
// Draw the entity.
cubeEntity.draw();

cubeEntity.draw()
{
	return cubeEntity .();
}
